{
  "name": "pocket-training",
  "version": "1.0.1",
  "description": "",
  "main": "develop.js",
  "scripts": {
    "build": "tsc && mv ./cloud/index.js ./cloud/main.js",
    "deploy": "b4a deploy",
    "dev": "node ./dev.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^5.8.1",
    "parse-server": "^6.2.2"
  },
  "devDependencies": {
    "@types/parse": "^3.0.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
